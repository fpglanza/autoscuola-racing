---
import { navigation } from "../../data/navigation";
import logoColor from "../../assets/logo/logo-r-color.png";

const desktopNavigation = navigation.filter((item) => item.href !== "/");

const leftNavigation = [
  desktopNavigation.find((item) => item.href === "/chi-siamo"),
  desktopNavigation.find((item) => item.href === "/patente-auto"),
  desktopNavigation.find((item) => item.href === "/pratiche"),
].filter(Boolean);

const rightNavigation = [
  desktopNavigation.find((item) => item.href === "/patenti"),
  desktopNavigation.find((item) => item.href === "/scuola-nautica"),
  desktopNavigation.find((item) => item.href === "/contatti"),
].filter(Boolean);
---

<header class="sticky top-0 z-50 bg-transparent">
  <div class="container py-4 md:py-5">
    <!-- DESKTOP -->
    <div class="hidden lg:block">
      <div class="rounded-full bg-[var(--color-secondary)] px-10 py-4 shadow-[0_16px_40px_rgba(0,0,0,0.20)]">
        <div class="grid grid-cols-[1fr_auto_1fr] items-center gap-8">
          <nav class="flex items-center justify-end gap-8">
            {leftNavigation.map((item) => (
              <a
                href={item.href}
                class="text-[12px] font-bold uppercase tracking-[0.08em] transition-all duration-200 hover:-translate-y-[1px] hover:text-white"
                style="color: #D4FF00;"
              >
                {item.label}
              </a>
            ))}
          </nav>

          <a href="/" class="mx-2 flex items-center justify-center">
            <img
              src={logoColor.src}
              alt="Autoscuola Racing"
              class="h-11 w-auto"
            />
          </a>

          <nav class="flex items-center justify-start gap-8">
            {rightNavigation.map((item) => (
              <a
                href={item.href}
                class="text-[13px] font-bold uppercase tracking-[0.08em] transition-all duration-200 hover:-translate-y-[1px] hover:text-white"
                style="color: #D4FF00;"
              >
                {item.label}
              </a>
            ))}
          </nav>
        </div>
      </div>
    </div>

    <!-- MOBILE / TABLET -->
    <div class="lg:hidden">
      <div class="flex items-center justify-between rounded-full bg-[var(--color-secondary)] px-5 py-3 shadow-[0_16px_40px_rgba(0,0,0,0.20)]">
        <a href="/" class="flex items-center">
          <img
            src={logoColor.src}
            alt="Autoscuola Racing"
            class="h-8 w-auto"
          />
        </a>

        <button
          id="menu-toggle"
          type="button"
          class="flex h-10 w-10 items-center justify-center rounded-full border border-white/10"
          aria-label="Apri menu"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <span class="flex flex-col gap-1.5">
            <span class="block h-0.5 w-5" style="background-color: #D4FF00;"></span>
            <span class="block h-0.5 w-5" style="background-color: #D4FF00;"></span>
            <span class="block h-0.5 w-5" style="background-color: #D4FF00;"></span>
          </span>
        </button>
      </div>

      <div
        id="mobile-menu"
        class="mt-3 hidden overflow-hidden rounded-3xl border border-black/5 bg-white shadow-[0_16px_40px_rgba(0,0,0,0.08)]"
      >
        <nav class="flex flex-col p-5">
          {navigation.map((item) => (
            <a
              href={item.href}
              class="border-b border-black/5 py-4 text-base font-bold uppercase tracking-[0.08em] text-[var(--color-secondary)] last:border-0"
            >
              {item.label}
            </a>
          ))}
        </nav>

        <div class="px-5 pb-5 pt-2">
          <a href="/contatti" class="btn-racing-primary w-full justify-center">
            contattaci
          </a>
        </div>
      </div>
    </div>
  </div>
</header>

<script>
  const toggle = document.getElementById("menu-toggle");
  const menu = document.getElementById("mobile-menu");

  toggle?.addEventListener("click", () => {
    const isExpanded = toggle.getAttribute("aria-expanded") === "true";
    toggle.setAttribute("aria-expanded", String(!isExpanded));
    menu?.classList.toggle("hidden");
  });
</script>